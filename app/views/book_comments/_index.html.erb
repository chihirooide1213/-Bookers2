
 <table class="table table-hover table-inverse">
      <% book_comments.each do |book_comment| %>
          <tr>
              <td><%= attachment_image_tag book_comment.user, :profile_image, :fill, 50, 50, format: 'jpeg', fallback: "no_image.jpg", size: '50x50' %></td>
              <td><%= book_comment.user.name %></td>
              <td><%= book_comment.comment %></td>
              <td><% if book_comment.user == current_user %>
                   <%= link_to 'Destroy', book_book_comment_path(book.id,book_comment.id), method: :delete, remote: true %> 
                  <% end %>
              </td>
            </tr>
      <% end %>
  </table>